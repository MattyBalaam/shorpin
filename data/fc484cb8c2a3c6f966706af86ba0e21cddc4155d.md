# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: list.spec.ts >> a concurrent addition from another device survives reconnect sync
- Location: integration-tests/list.spec.ts:290:1

# Error details

```
Error: page.waitForResponse: Target page, context or browser has been closed
```

# Page snapshot

```yaml
- main [ref=e2]:
  - navigation "Breadcrumbs" [ref=e3]:
    - list [ref=e4]:
      - listitem [ref=e5]:
        - link "Back to index" [ref=e6] [cursor=pointer]:
          - /url: /
          - generic [ref=e7]: Back to index
          - generic [ref=e8]: ⟵
      - listitem [ref=e9]:
        - heading "Shopping" [level=1] [ref=e10]
  - button "🎨" [ref=e12]
  - generic [ref=e13]:
    - button "Update" [ref=e15]
    - list [ref=e18]:
      - listitem [ref=e19]:
        - generic [ref=e21]:
          - textbox "Edit Milk" [ref=e22]: Milk
          - generic "Reorder Milk" [ref=e23]:
            - generic [ref=e24]: Reorder Milk
            - generic [ref=e25]: "|-|-|-|-|"
          - button "Delete Milk ✕" [ref=e27] [cursor=pointer]:
            - generic [ref=e28]: Delete Milk
            - text: ✕
      - listitem [ref=e29]:
        - generic [ref=e31]:
          - textbox "Edit Bread" [ref=e32]: Bread
          - generic "Reorder Bread" [ref=e33]:
            - generic [ref=e34]: Reorder Bread
            - generic [ref=e35]: "|-|-|-|-|"
          - button "Delete Bread ✕" [ref=e37] [cursor=pointer]:
            - generic [ref=e38]: Delete Bread
            - text: ✕
      - listitem [ref=e39]:
        - generic [ref=e41]:
          - textbox "Edit Eggs" [ref=e42]: Eggs
          - generic "Reorder Eggs" [ref=e43]:
            - generic [ref=e44]: Reorder Eggs
            - generic [ref=e45]: "|-|-|-|-|"
          - button "Delete Eggs ✕" [ref=e47] [cursor=pointer]:
            - generic [ref=e48]: Delete Eggs
            - text: ✕
      - listitem [ref=e49]:
        - generic [ref=e51]:
          - textbox "Edit Butter" [ref=e52]: Butter
          - generic "Reorder Butter" [ref=e53]:
            - generic [ref=e54]: Reorder Butter
            - generic [ref=e55]: "|-|-|-|-|"
          - button "Delete Butter ✕" [ref=e57] [cursor=pointer]:
            - generic [ref=e58]: Delete Butter
            - text: ✕
      - listitem [ref=e59]:
        - generic [ref=e61]:
          - textbox "Edit Cheese" [ref=e62]: Cheese
          - generic "Reorder Cheese" [ref=e63]:
            - generic [ref=e64]: Reorder Cheese
            - generic [ref=e65]: "|-|-|-|-|"
          - button "Delete Cheese ✕" [ref=e67] [cursor=pointer]:
            - generic [ref=e68]: Delete Cheese
            - text: ✕
      - listitem [ref=e69]:
        - generic [ref=e71]:
          - textbox "Edit Bananas" [ref=e72]: Bananas
          - generic "Reorder Bananas" [ref=e73]:
            - generic [ref=e74]: Reorder Bananas
            - generic [ref=e75]: "|-|-|-|-|"
          - button "Delete Bananas ✕" [ref=e77] [cursor=pointer]:
            - generic [ref=e78]: Delete Bananas
            - text: ✕
      - listitem [ref=e79]:
        - generic [ref=e81]:
          - textbox "Edit Apples" [ref=e82]: Apples
          - generic "Reorder Apples" [ref=e83]:
            - generic [ref=e84]: Reorder Apples
            - generic [ref=e85]: "|-|-|-|-|"
          - button "Delete Apples ✕" [ref=e87] [cursor=pointer]:
            - generic [ref=e88]: Delete Apples
            - text: ✕
      - listitem [ref=e89]:
        - generic [ref=e91]:
          - textbox "Edit Yogurt" [ref=e92]: Yogurt
          - generic "Reorder Yogurt" [ref=e93]:
            - generic [ref=e94]: Reorder Yogurt
            - generic [ref=e95]: "|-|-|-|-|"
          - button "Delete Yogurt ✕" [ref=e97] [cursor=pointer]:
            - generic [ref=e98]: Delete Yogurt
            - text: ✕
      - listitem [ref=e99]:
        - generic [ref=e101]:
          - textbox "Edit Orange juice" [ref=e102]: Orange juice
          - generic "Reorder Orange juice" [ref=e103]:
            - generic [ref=e104]: Reorder Orange juice
            - generic [ref=e105]: "|-|-|-|-|"
          - button "Delete Orange juice ✕" [ref=e107] [cursor=pointer]:
            - generic [ref=e108]: Delete Orange juice
            - text: ✕
      - listitem [ref=e109]:
        - generic [ref=e111]:
          - textbox "Edit Coffee" [ref=e112]: Coffee
          - generic "Reorder Coffee" [ref=e113]:
            - generic [ref=e114]: Reorder Coffee
            - generic [ref=e115]: "|-|-|-|-|"
          - button "Delete Coffee ✕" [ref=e117] [cursor=pointer]:
            - generic [ref=e118]: Delete Coffee
            - text: ✕
      - listitem [ref=e119]:
        - generic [ref=e121]:
          - textbox "Edit Margarine" [ref=e122]: Margarine
          - generic "Reorder Margarine" [ref=e123]:
            - generic [ref=e124]: Reorder Margarine
            - generic [ref=e125]: "|-|-|-|-|"
          - button "Delete Margarine ✕" [ref=e127] [cursor=pointer]:
            - generic [ref=e128]: Delete Margarine
            - text: ✕
    - generic [ref=e130]:
      - generic [ref=e131]: New item
      - textbox "New item" [ref=e132]
      - button "Add" [active] [ref=e133]
  - region "Notifications alt+T" [ref=e134]
```

# Test source

```ts
  228 |   // reached the server, not just the most recent one. mutate_list previously
  229 |   // only ever inserted a single new row per resync request (see migration
  230 |   // 20260806000000), so before that fix only "Offline sugar" would persist.
  231 |   await openList(page, "shopping");
  232 |   await expect(page.getByLabel("Edit Offline flour")).toBeVisible();
  233 |   await expect(page.getByLabel("Edit Offline sugar")).toBeVisible();
  234 | });
  235 | 
  236 | test("a stale-session redirect mid-sync doesn't drop the queued edit, and it flushes once retried", async ({
  237 |   page,
  238 |   ctx,
  239 |   context,
  240 | }) => {
  241 |   await login(page, ctx.ownerEmail);
  242 |   await openList(page, "shopping");
  243 |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  244 | 
  245 |   await context.setOffline(true);
  246 |   await page.getByLabel("New item").fill("Offline butter");
  247 |   await page.getByRole("button", { name: "Add" }).click();
  248 |   await expect(page.getByLabel("Edit Offline butter")).toBeVisible({ timeout: 10000 });
  249 | 
  250 |   // The rebase-and-resubmit sync goes through RR's single-fetch protocol
  251 |   // (submit(), not a raw fetch), so a middleware redirect arrives encoded
  252 |   // as a 204 + X-Remix-Redirect/-Status headers, not a raw 3xx — see
  253 |   // fetchAndDecodeViaTurboStream in react-router's single-fetch client.
  254 |   // This is what supabaseMiddleware's redirect(href("/login")) actually
  255 |   // produces for a stale/unrefreshable session.
  256 |   let redirectedPostSeen = false;
  257 |   await page.route("**/lists/shopping.data", async (route) => {
  258 |     if (route.request().method() === "POST") {
  259 |       redirectedPostSeen = true;
  260 |       await route.fulfill({
  261 |         status: 204,
  262 |         headers: { "X-Remix-Redirect": "/login", "X-Remix-Status": "302" },
  263 |       });
  264 |       return;
  265 |     }
  266 |     await route.continue();
  267 |   });
  268 | 
  269 |   await context.setOffline(false);
  270 |   await expect(page.getByText("Back online - syncing changes")).toBeVisible();
  271 |   await expect.poll(() => redirectedPostSeen).toBe(true);
  272 |   // React Router follows the redirect as a normal completed navigation.
  273 |   await page.waitForURL("/login");
  274 | 
  275 |   await page.unroute("**/lists/shopping.data");
  276 | 
  277 |   // Log back in and return — no further online/offline transition occurs
  278 |   // here, so only the mount-time retry (not just onOnline) can pick the
  279 |   // still-queued edit back up.
  280 |   await login(page, ctx.ownerEmail);
  281 |   await openList(page, "shopping");
  282 |   await expect(page.getByLabel("Edit Offline butter")).toBeVisible({ timeout: 10000 });
  283 | 
  284 |   // Must survive a fresh reload — proves it actually reached the server
  285 |   // this time, not just optimistic local state.
  286 |   await openList(page, "shopping");
  287 |   await expect(page.getByLabel("Edit Offline butter")).toBeVisible();
  288 | });
  289 | 
  290 | test("a concurrent addition from another device survives reconnect sync", async ({
  291 |   browser,
  292 |   ctx,
  293 | }) => {
  294 |   const ownerContext = await browser.newContext({ baseURL });
  295 |   const collabContext = await browser.newContext({ baseURL });
  296 |   try {
  297 |     const ownerPage = await ownerContext.newPage();
  298 |     const collabPage = await collabContext.newPage();
  299 | 
  300 |     await login(ownerPage, ctx.ownerEmail);
  301 |     await login(collabPage, ctx.collabEmail);
  302 | 
  303 |     await openList(ownerPage, "shopping");
  304 | 
  305 |     await ownerContext.setOffline(true);
  306 |     await expect(ownerPage.getByText("Offline", { exact: true })).toBeVisible();
  307 | 
  308 |     // Owner deletes Milk while offline — a pure edit/delete, no addition, so
  309 |     // this isolates the concurrency behaviour from mutate_list's "only the
  310 |     // most recent offline addition survives" limitation (see the comment on
  311 |     // performRebaseSync in list.tsx). Generous timeouts throughout this test:
  312 |     // under full-suite load these client-side/animation-dependent steps have
  313 |     // been observed occasionally slow, consistent with resource contention
  314 |     // rather than a functional issue — reliable and near-instant in isolation.
  315 |     await ownerPage.getByRole("button", { name: "Delete Milk" }).click();
  316 |     await expect(ownerPage.getByLabel("Edit Milk")).not.toBeVisible({ timeout: 10000 });
  317 | 
  318 |     // Meanwhile, another device (online) adds an item to the same list.
  319 |     // Replaying the owner's stale full-array snapshot naively would make
  320 |     // mutate_list treat Margarine as "not in what I submitted" and
  321 |     // soft-delete it — this is exactly the bug rebaseListItems exists to
  322 |     // avoid.
  323 |     await openList(collabPage, "shopping");
  324 |     await collabPage.getByLabel("New item").fill("Margarine");
  325 |     await collabPage.getByRole("button", { name: "Add" }).click();
  326 |     await expect(collabPage.getByLabel("Edit Margarine")).toBeVisible({ timeout: 10000 });
  327 | 
> 328 |     const syncSubmitted = ownerPage.waitForResponse(
      |                                     ^ Error: page.waitForResponse: Target page, context or browser has been closed
  329 |       (response) =>
  330 |         response.url().includes("/lists/shopping.data") && response.request().method() === "POST",
  331 |     );
  332 | 
  333 |     await ownerContext.setOffline(false);
  334 |     await expect(ownerPage.getByText("Back online - syncing changes")).toBeVisible();
  335 |     await syncSubmitted;
  336 |     await expect(ownerPage.getByText("Offline", { exact: true })).not.toBeVisible();
  337 | 
  338 |     // Reload to confirm both changes actually reached the server: the
  339 |     // collaborator's concurrent addition survived, and the owner's own
  340 |     // offline deletion was applied.
  341 |     await openList(ownerPage, "shopping");
  342 |     await expect(ownerPage.getByLabel("Edit Margarine")).toBeVisible();
  343 |     await expect(ownerPage.getByLabel("Edit Milk")).not.toBeVisible();
  344 |   } finally {
  345 |     await ownerContext.close();
  346 |     await collabContext.close();
  347 |   }
  348 | });
  349 | 
  350 | test("own changes do not trigger the updated-by-another-user notification", async ({
  351 |   browser,
  352 |   ctx,
  353 | }) => {
  354 |   const ownerContext = await browser.newContext({ baseURL });
  355 |   const collabContext = await browser.newContext({ baseURL });
  356 |   try {
  357 |     const ownerPage = await ownerContext.newPage();
  358 |     const collabPage = await collabContext.newPage();
  359 | 
  360 |     await login(ownerPage, ctx.ownerEmail);
  361 |     await login(collabPage, ctx.collabEmail);
  362 | 
  363 |     await openList(ownerPage, "shopping");
  364 |     await openList(collabPage, "shopping");
  365 | 
  366 |     await ownerPage.getByLabel("New item").fill("Margarine");
  367 |     await ownerPage.getByRole("button", { name: "Add" }).click();
  368 | 
  369 |     // Anchor timing on actual broadcast delivery: the collaborator sees it...
  370 |     await expect(collabPage.getByText("List updated by another user")).toBeVisible();
  371 |     // ...while the owner — whose clientId matches the broadcast — must not.
  372 |     await expect(ownerPage.getByText("List updated by another user")).not.toBeVisible();
  373 |   } finally {
  374 |     await ownerContext.close();
  375 |     await collabContext.close();
  376 |   }
  377 | });
  378 | 
  379 | test("item added by collaborator appears as unread for owner on home page", async ({
  380 |   browser,
  381 |   ctx,
  382 | }) => {
  383 |   const ownerContext = await browser.newContext({ baseURL });
  384 |   const collabContext = await browser.newContext({ baseURL });
  385 |   try {
  386 |     const ownerPage = await ownerContext.newPage();
  387 |     const collabPage = await collabContext.newPage();
  388 | 
  389 |     await login(ownerPage, ctx.ownerEmail);
  390 |     await login(collabPage, ctx.collabEmail);
  391 | 
  392 |     // Owner opens the list — records viewed_at covering all current items
  393 |     await openList(ownerPage, "shopping");
  394 |     await expect(ownerPage.getByLabel("Edit Milk")).toBeVisible();
  395 | 
  396 |     // Owner goes back home — no unread badge because viewed_at > all item timestamps
  397 |     await ownerPage.goto("/");
  398 |     await expect(ownerPage.getByText(/unread/)).not.toBeVisible();
  399 | 
  400 |     // Collab adds a new item to the same list
  401 |     await openList(collabPage, "shopping");
  402 |     await collabPage.getByLabel("New item").fill("Margarine");
  403 |     await collabPage.getByRole("button", { name: "Add" }).click();
  404 |     await expect(collabPage.getByLabel("Edit Margarine")).toBeVisible();
  405 | 
  406 |     // Owner refreshes home — the new item's updated_at is after owner's viewed_at
  407 |     await ownerPage.reload();
  408 |     await expect(ownerPage.getByText("1 unread")).toBeVisible();
  409 |   } finally {
  410 |     await ownerContext.close();
  411 |     await collabContext.close();
  412 |   }
  413 | });
  414 | 
  415 | test("new item is marked in the list since last opening", async ({ browser, ctx }) => {
  416 |   const ownerContext = await browser.newContext({ baseURL });
  417 |   const collabContext = await browser.newContext({ baseURL });
  418 |   try {
  419 |     const ownerPage = await ownerContext.newPage();
  420 |     const collabPage = await collabContext.newPage();
  421 | 
  422 |     await login(ownerPage, ctx.ownerEmail);
  423 |     await login(collabPage, ctx.collabEmail);
  424 | 
  425 |     await openList(ownerPage, "shopping");
  426 |     await expect(ownerPage.getByLabel("Edit Milk")).toBeVisible();
  427 | 
  428 |     await ownerPage.goto("/");
```