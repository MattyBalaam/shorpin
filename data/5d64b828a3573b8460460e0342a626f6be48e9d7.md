# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: list.spec.ts >> an item added offline syncs to the server on reconnect
- Location: integration-tests/list.spec.ts:158:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByLabel('Edit Offline butter')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByLabel('Edit Offline butter')

```

```yaml
- main:
  - text: Offline
  - navigation "Breadcrumbs":
    - list:
      - listitem:
        - link "Back to index":
          - /url: /
      - listitem:
        - heading "Shopping" [level=1]
  - button "🎨"
  - button "Update"
  - list:
    - listitem:
      - textbox "Edit Milk": Milk
      - text: new since last opening Reorder Milk
      - button "Delete Milk ✕"
    - listitem:
      - textbox "Edit Bread": Bread
      - text: new since last opening Reorder Bread
      - button "Delete Bread ✕"
    - listitem:
      - textbox "Edit Eggs": Eggs
      - text: new since last opening Reorder Eggs
      - button "Delete Eggs ✕"
    - listitem:
      - textbox "Edit Butter": Butter
      - text: new since last opening Reorder Butter
      - button "Delete Butter ✕"
    - listitem:
      - textbox "Edit Cheese": Cheese
      - text: new since last opening Reorder Cheese
      - button "Delete Cheese ✕"
    - listitem:
      - textbox "Edit Bananas": Bananas
      - text: new since last opening Reorder Bananas
      - button "Delete Bananas ✕"
    - listitem:
      - textbox "Edit Apples": Apples
      - text: new since last opening Reorder Apples
      - button "Delete Apples ✕"
    - listitem:
      - textbox "Edit Yogurt": Yogurt
      - text: new since last opening Reorder Yogurt
      - button "Delete Yogurt ✕"
    - listitem:
      - textbox "Edit Orange juice": Orange juice
      - text: new since last opening Reorder Orange juice
      - button "Delete Orange juice ✕"
    - listitem:
      - textbox "Edit Coffee": Coffee
      - text: new since last opening Reorder Coffee
      - button "Delete Coffee ✕"
  - text: New item
  - textbox "New item"
  - button "Add"
  - region "Notifications alt+T"
```

# Test source

```ts
  72  | 
  73  |   // No recreate affordance until something has been deleted this session
  74  |   await expect(page.getByRole("button", { name: "Recreate last deleted" })).toHaveCount(0);
  75  | 
  76  |   await page.getByRole("button", { name: "Delete Milk" }).click();
  77  | 
  78  |   await expect(page.getByLabel("Edit Milk")).not.toBeVisible();
  79  |   await expect(page.getByRole("button", { name: "Recreate last deleted" })).toBeVisible();
  80  |   // Deliberately not an undo — no server undelete round-trip
  81  |   await expect(page.getByRole("button", { name: "Undo" })).toHaveCount(0);
  82  | });
  83  | 
  84  | test("'Recreate last deleted' re-adds the deleted value as a fresh item", async ({ page, ctx }) => {
  85  |   await login(page, ctx.ownerEmail);
  86  |   await openList(page, "shopping");
  87  | 
  88  |   await page.getByRole("button", { name: "Delete Milk" }).click();
  89  |   await expect(page.getByLabel("Edit Milk")).not.toBeVisible();
  90  | 
  91  |   await page.getByRole("button", { name: "Recreate last deleted" }).click();
  92  | 
  93  |   // Milk returns (as a freshly-added item at the end of the list)
  94  |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  95  |   // The affordance clears once the value has been recreated
  96  |   await expect(page.getByRole("button", { name: "Recreate last deleted" })).toHaveCount(0);
  97  | });
  98  | 
  99  | test("edited icon does not appear on unedited items while another item is being deleted", async ({
  100 |   page,
  101 |   ctx,
  102 | }) => {
  103 |   await login(page, ctx.ownerEmail);
  104 |   await openList(page, "shopping");
  105 | 
  106 |   // Edit Milk without submitting, giving it edited=true
  107 |   await page.getByLabel("Edit Milk").fill("Oat Milk");
  108 | 
  109 |   // Hold the revalidation loader so the navigation stays in "loading" state
  110 |   // long enough to assert — this is when navigation.formData becomes null and
  111 |   // isSavingEdit incorrectly flips true for all edited items.
  112 |   const { intercepted, release } = await holdLoader(page, /\/lists\/shopping\.data/);
  113 | 
  114 |   // Delete a different item
  115 |   await page.getByRole("button", { name: "Delete Bread" }).click();
  116 | 
  117 |   // Wait until the GET revalidation is actually intercepted before asserting
  118 |   await intercepted;
  119 | 
  120 |   // While in "loading" state no saving indicator should appear — we are not
  121 |   // persisting any edit, we are only deleting Bread
  122 |   await expect(
  123 |     page.locator("li").filter({ has: page.getByLabel("Edit Oat Milk") }),
  124 |   ).not.toContainText("saving");
  125 | 
  126 |   release();
  127 |   await expect(page.getByRole("button", { name: "Recreate last deleted" })).toBeVisible();
  128 | 
  129 |   // The delete form submission also saves all current item values (including
  130 |   // "Oat Milk"), so at idle there is no pending unsaved edit — the edited
  131 |   // indicator should not appear for any item.
  132 |   await expect(
  133 |     page.locator("li").filter({ has: page.getByLabel("Edit Oat Milk") }),
  134 |   ).not.toContainText(" edited");
  135 |   await expect(page.locator("li").filter({ has: page.getByLabel("Edit Eggs") })).not.toContainText(
  136 |     " edited",
  137 |   );
  138 | });
  139 | 
  140 | test("when offline, the offline indicator appears and cached list data remains visible", async ({
  141 |   page,
  142 |   ctx,
  143 |   context,
  144 | }) => {
  145 |   await login(page, ctx.ownerEmail);
  146 |   await openList(page, "shopping");
  147 |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  148 | 
  149 |   await context.setOffline(true);
  150 | 
  151 |   await expect(page.getByText("Offline", { exact: true })).toBeVisible();
  152 |   // Items should remain visible from the clientLoader cache
  153 |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  154 |   await expect(page.getByLabel("Edit Bread")).toBeVisible();
  155 |   await expect(page.getByLabel("Edit Eggs")).toBeVisible();
  156 | });
  157 | 
  158 | test("an item added offline syncs to the server on reconnect", async ({ page, ctx, context }) => {
  159 |   await login(page, ctx.ownerEmail);
  160 |   await openList(page, "shopping");
  161 |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  162 | 
  163 |   await context.setOffline(true);
  164 |   await expect(page.getByText("Offline", { exact: true })).toBeVisible();
  165 | 
  166 |   // Add while offline — the clientAction stores it locally. Generous
  167 |   // timeout: under full-suite load this client-side-only step has been
  168 |   // observed occasionally slow, consistent with resource contention rather
  169 |   // than a functional issue — it's instant and 100% reliable in isolation.
  170 |   await page.getByLabel("New item").fill("Offline butter");
  171 |   await page.getByRole("button", { name: "Add" }).click();
> 172 |   await expect(page.getByLabel("Edit Offline butter")).toBeVisible({ timeout: 10000 });
      |                                                        ^ Error: expect(locator).toBeVisible() failed
  173 | 
  174 |   // Reconnect sync fetches fresh server state before rebasing and
  175 |   // resubmitting (so it doesn't resurrect anything changed concurrently by
  176 |   // another device) — wait for that resubmission to actually land before
  177 |   // reloading, rather than just the "syncing" toast, since a full reload
  178 |   // aborts any request still in flight.
  179 |   const syncSubmitted = page.waitForResponse(
  180 |     (response) =>
  181 |       response.url().includes("/lists/shopping.data") && response.request().method() === "POST",
  182 |   );
  183 | 
  184 |   await context.setOffline(false);
  185 | 
  186 |   // onOnline fires: resubmit persists the local edits, indicator clears
  187 |   await expect(page.getByText("Back online - syncing changes")).toBeVisible();
  188 |   await syncSubmitted;
  189 |   await expect(page.getByText("Offline", { exact: true })).not.toBeVisible();
  190 | 
  191 |   // The offline item must survive a full reload — i.e. it reached the server
  192 |   await openList(page, "shopping");
  193 |   await expect(page.getByLabel("Edit Offline butter")).toBeVisible();
  194 | });
  195 | 
  196 | test("multiple items added offline all sync to the server on reconnect", async ({
  197 |   page,
  198 |   ctx,
  199 |   context,
  200 | }) => {
  201 |   await login(page, ctx.ownerEmail);
  202 |   await openList(page, "shopping");
  203 |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  204 | 
  205 |   await context.setOffline(true);
  206 |   await expect(page.getByText("Offline", { exact: true })).toBeVisible();
  207 | 
  208 |   await page.getByLabel("New item").fill("Offline flour");
  209 |   await page.getByRole("button", { name: "Add" }).click();
  210 |   await expect(page.getByLabel("Edit Offline flour")).toBeVisible({ timeout: 10000 });
  211 | 
  212 |   await page.getByLabel("New item").fill("Offline sugar");
  213 |   await page.getByRole("button", { name: "Add" }).click();
  214 |   await expect(page.getByLabel("Edit Offline sugar")).toBeVisible({ timeout: 10000 });
  215 | 
  216 |   const syncSubmitted = page.waitForResponse(
  217 |     (response) =>
  218 |       response.url().includes("/lists/shopping.data") && response.request().method() === "POST",
  219 |   );
  220 | 
  221 |   await context.setOffline(false);
  222 | 
  223 |   await expect(page.getByText("Back online - syncing changes")).toBeVisible();
  224 |   await syncSubmitted;
  225 |   await expect(page.getByText("Offline", { exact: true })).not.toBeVisible();
  226 | 
  227 |   // Both offline additions must survive a full reload — i.e. both actually
  228 |   // reached the server, not just the most recent one. mutate_list previously
  229 |   // only ever inserted a single new row per resync request (see migration
  230 |   // 20260806000000), so before that fix only "Offline sugar" would persist.
  231 |   await openList(page, "shopping");
  232 |   await expect(page.getByLabel("Edit Offline flour")).toBeVisible();
  233 |   await expect(page.getByLabel("Edit Offline sugar")).toBeVisible();
  234 | });
  235 | 
  236 | test("a stale-session redirect mid-sync doesn't drop the queued edit, and it flushes once retried", async ({
  237 |   page,
  238 |   ctx,
  239 |   context,
  240 | }) => {
  241 |   await login(page, ctx.ownerEmail);
  242 |   await openList(page, "shopping");
  243 |   await expect(page.getByLabel("Edit Milk")).toBeVisible();
  244 | 
  245 |   await context.setOffline(true);
  246 |   await page.getByLabel("New item").fill("Offline butter");
  247 |   await page.getByRole("button", { name: "Add" }).click();
  248 |   await expect(page.getByLabel("Edit Offline butter")).toBeVisible({ timeout: 10000 });
  249 | 
  250 |   // The rebase-and-resubmit sync goes through RR's single-fetch protocol
  251 |   // (submit(), not a raw fetch), so a middleware redirect arrives encoded
  252 |   // as a 204 + X-Remix-Redirect/-Status headers, not a raw 3xx — see
  253 |   // fetchAndDecodeViaTurboStream in react-router's single-fetch client.
  254 |   // This is what supabaseMiddleware's redirect(href("/login")) actually
  255 |   // produces for a stale/unrefreshable session.
  256 |   let redirectedPostSeen = false;
  257 |   await page.route("**/lists/shopping.data", async (route) => {
  258 |     if (route.request().method() === "POST") {
  259 |       redirectedPostSeen = true;
  260 |       await route.fulfill({
  261 |         status: 204,
  262 |         headers: { "X-Remix-Redirect": "/login", "X-Remix-Status": "302" },
  263 |       });
  264 |       return;
  265 |     }
  266 |     await route.continue();
  267 |   });
  268 | 
  269 |   await context.setOffline(false);
  270 |   await expect(page.getByText("Back online - syncing changes")).toBeVisible();
  271 |   await expect.poll(() => redirectedPostSeen).toBe(true);
  272 |   // React Router follows the redirect as a normal completed navigation.
```